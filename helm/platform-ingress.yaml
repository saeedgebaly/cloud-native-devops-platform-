apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: platform-ingress
  namespace: dev
spec:
  ingressClassName: nginx
  rules:
    - host: dev.52.174.76.163.nip.io
      http:
        paths:
          - path: /auth
            pathType: Prefix
            backend:
              service:
                name: auth-service-dev
                port:
                  number: 3000

          - path: /core
            pathType: Prefix
            backend:
              service:
                name: core-service-dev
                port:
                  number: 3000

          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80

