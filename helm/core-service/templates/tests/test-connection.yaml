apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "core-service.fullname" . }}-test-connection"
  labels:
    {{- include "core-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "core-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
