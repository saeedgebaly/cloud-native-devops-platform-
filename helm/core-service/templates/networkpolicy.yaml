{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: core-network-policy
spec:
  podSelector:
    matchLabels:
      app: core-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
{{- end }}
